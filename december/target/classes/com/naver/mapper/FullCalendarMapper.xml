<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mqapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.mapper.FullCalendarMapper">

	<insert id="insert">
		INSERT INTO tbl_fullCalendar(username, title, start, end, backgroundColor)
		VALUES(#{username}, #{title}, #{start}, #{end}, #{backgroundColor});
	</insert>
	
	<select id="getListPage" resultType="com.naver.domain.FullCalendarVO">
		SELECT * FROM tbl_fullCalendar;
	</select>
	
	
	<select id="read" resultType="com.naver.domain.FullCalendarVO">
		SELECT * FROM tbl_fullCalendar WHERE id = #{id}
	</select>
	
	<update id="update">
		UPDATE tbl_fullCalendar SET title = #{title}, start = #{start}, end = #{end}, 
		backgroundColor = #{backgroundColor}
		WHERE id = #{id}
	</update>
	
	<delete id="delete">
		DELETE FROM tbl_fullCalendar WHERE 1
	</delete>
	
	<delete id="deleteMonth">
		DELETE FROM tbl_fullCalendar WHERE start LIKE concat(#{startSub}, '%')
	</delete>
	
</mapper>



